[user]
	name = Masahiro Furudate
	email = 178inaba.git@gmail.com
[core]
	editor = vim
	pager = diff-so-fancy | less --tabs=4 -RF
	quotepath = false
	# core.excludesFile を明示しないと XDG_CONFIG_HOME を設定したマシンで
	# 参照先が動き、stow の配置先 (~/.config/git/ignore) が読まれなくなる。
	# ignore が効かなくなってもエラーは出ず status が汚れるだけで気づけない
	# ため、デフォルトと同値でも明示する。
	excludesFile = ~/.config/git/ignore
[interactive]
	diffFilter = diff-so-fancy --patch
[color]
	ui = true
[color "diff-highlight"]
	oldNormal = red bold
	oldHighlight = red bold 52
	newNormal = green bold
	newHighlight = green bold 22
[color "diff"]
	meta = 11
	frag = magenta bold
	func = 146 bold
	commit = yellow bold
	old = red bold
	new = green bold
	whitespace = red reverse
[url "git@bitbucket.org:"]
	insteadOf = https://bitbucket.org/
[url "git@github.com:"]
	insteadOf = https://github.com/
[alias]
	cc = checkout .

	# codereview
	change = codereview change
	gofmt = codereview gofmt
	mail = codereview mail
	pending = codereview pending
	submit = codereview submit
	sync = codereview sync
[http]
	cookiefile = ~/.gitcookies
[init]
	defaultBranch = main
[branch]
	# origin/main から切ったブランチが origin/main を追うと、引数なし push が
	# 名前不一致で落ち、`@{u}` の有無で「一度も push していない」を判定できなくなる
	# （消費者は claude/.claude/skills/git-rebase/SKILL.md のステップ15）。
	autoSetupMerge = simple
[pull]
	rebase = false
[push]
	# 許可ルール `Bash(git push)` が「引数なし push は同名のリモートブランチだけを
	# 非 force で進める」ことに依存する（設計意図は claude/.claude/rules/hooks-design.md）。
	# default は既定と同値でも明示し、autoSetupRemote が初回 push の upstream を張る。
	default = simple
	autoSetupRemote = true
[fetch]
	prune = true
# remote.origin.* をここに直接書くと remote 未設定のリポジトリにも幽霊 origin が
# 現れる（git remote に列挙され fetch が silent no-op 化する）ため、
# remote が実在するリポジトリだけに適用する。hasconfig はリモート名では絞れない
# （remote.*.url の * は固定構文）ので、origin 以外のリモートしか無いリポジトリに
# 幽霊 origin が出る残存ケースは許容
[includeIf "hasconfig:remote.*.url:**"]
	path = ~/.gitconfig.remote
[include]
	path = ~/.gitconfig.local
