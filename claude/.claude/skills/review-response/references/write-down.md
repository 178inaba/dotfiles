# 決定の書き下ろし

設計思想に関わる指摘で決定の記録が無く、ユーザーが意図を確認したときに読む。書き下ろし先は 2 つ:

- スコープ・方針に関する決定 → PR 本文。**節だけ**を `work_dir` 直下に Write し、`ccx pr body-append <ドキュメント> --body-file <work_dir 直下のファイル名>` で追記する。他人の PR では本文を編集せず、下のコードコメントかエスカレーションのまま留める
- コードの将来の読者が必要とする理由 → コードコメント（*why* を書く）。コミットして push する
