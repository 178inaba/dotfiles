# AGENTS.md

OpenCodeでこのリポジトリを扱う際の追加指示。

## 言語

- ユーザーとのやりとりは原則として日本語で行う
- コミットメッセージ、コード、設定キー、外部仕様の引用は必要に応じて英語を使ってよい
- 既存ファイル内のコメントや文体が英語の場合は、そのファイルの慣習を優先する

## 設定ファイルの扱い

- `~/.config/opencode/` 配下はStowで配置される前提のため、ホーム側ではなく `opencode/.config/opencode/` を編集する
